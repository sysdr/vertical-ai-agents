version: '3.8'

services:
  l6-api-client:
    build: .
    ports:
      - "8000:8000"
      - "3000:3000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./logs:/app/logs
